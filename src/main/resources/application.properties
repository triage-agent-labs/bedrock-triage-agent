spring.application.name=bedrock-triage-agent
spring.thymeleaf.cache=false

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,beans,mappings,env
management.endpoint.health.probes.enabled=true
